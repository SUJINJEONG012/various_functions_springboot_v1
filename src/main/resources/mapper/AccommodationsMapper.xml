<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.various_functions.admin.mapper.AccommodationsMapper">

<!-- 숙소저장 -->
<insert id="save" parameterType="com.various_functions.admin.dto.AccommodationsDto">

insert into accommodations (aid, acate, aname, aadress, aphone, axcoordi, atotalroom, agrade, adetail, amainimg, aregdate)
values(#{aid}, #{acate},#{aname},#{aadress},#{aphone},#{axcoordi},#{atotalroom},#{agrade},#{adetail}, #{amainimg},now())

</insert>

<!--  숙소 리스트 -->
<select id="getAllAccommodations" resultType="com.various_functions.admin.vo.AccommodationsVo">
select * from accommodations;
</select>
</mapper>